# Widget Service

## Build

Run:

```sh
make build
```

This fixture is intentionally small. It gives artifact.autopsy.inspect a
documentation claim to compare against the adjacent Makefile without executing
either command.
