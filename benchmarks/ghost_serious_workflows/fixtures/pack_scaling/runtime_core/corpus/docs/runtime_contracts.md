# Runtime Contracts

The runtime worker sync path is the contract surface for project truth.
Worker sync must stay bounded by queue_guard and candidate_surfaces.
Grounding should point at runtime facts before UI notes or noisy logs.
