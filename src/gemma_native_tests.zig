const agents = @import("gemma/agents.zig");
const forward_schedule = @import("gemma/forward_schedule.zig");
const inference = @import("gemma/inference.zig");

test {
    _ = agents;
    _ = forward_schedule;
    _ = inference;
}
